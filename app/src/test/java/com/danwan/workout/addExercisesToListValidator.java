package com.danwan.workout;

import org.junit.Test;

import static org.junit.Assert.assertFalse;
import static org.junit.Assert.assertTrue;

/**
 * To work on unit tests, switch the Test Artifact in the Build Variants view.
 */
public class addExercisesToListValidator {

    private String validName = "Push Ups";


    /*No invalid names for exercise names at the moment
    private String invalidName = "";
   */

    @Test
    public void verifyExerciseNameAdded_verified_returnsTrue(){

        onView(withId(R.id.))
    }



    //if current set is x, then when timer finishes, current set is x+1

    //if
}